{
    "name": "bones",
    "version": "1.0.0",

    "main": "./bones.js",

    "dependencies": {
        "underscore": ">= 1.1.6 < 1.2.0",
        "express": ">= 2.2.2 < 2.3.0",
        "backbone": ">= 0.3.3 < 0.4.0",
        "jquery": ">= 1.5.1 < 1.6.0",
        "optimist": ">= 0.1.9 < 0.2.0",
        "mirror": ">= 0.1.0 < 0.2.0"
    },

    "scripts": {
        "test": "expresso"
    }
}
