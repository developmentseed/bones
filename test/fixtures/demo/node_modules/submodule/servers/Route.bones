servers['Route'].augment({
    initialize: function(parent, app) {
        parent.call(this, app);
        this.get('/submodule-page', this.submodulePage);
    }
});

servers.Route.prototype.submodulePage = function(req, res, next) {
    res.send('submodule page');
};
