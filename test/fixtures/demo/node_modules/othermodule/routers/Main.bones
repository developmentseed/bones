router = Bones.Router.extend({
    initialize: function() {
        this.server.get('/submodule-page', this.submodulePage);
    },

    submodulePage: function(req, res, next) {
        res.send('submodule page');
    }
});
