router = (routers.foo || Backbone.Router).extend({
    routes: {
        '/foo': 'foo'
    },

    foo: function(res) {
        res.send('foo');
    }
}    , {
        'extended': 'submodule'
    });
